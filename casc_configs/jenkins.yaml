credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: "github-creds"
              secret: "${GITHUB_PAT}"
              description: "GitHub Personal Access Token"
